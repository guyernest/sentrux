---
phase: 02.1-rust-deep-analysis
plan: 02
subsystem: analysis, renderer
tags: [graph-metrics, coverage, clippy, scan-pipeline, render-context, color-mode, rust]

requires:
  - phase: 02.1-rust-deep-analysis
    plan: 01
    provides: "GraphMetricsReport/CoverageReport/ClippyReport types, run_graph_metrics/run_coverage/run_clippy_warnings adapters, coverage_color/risk_color functions, ColorMode::Coverage/Risk variants"

provides:
  - "ScanReports with graph_metrics and clippy fields (both populated during scan)"
  - "ScanMsg::CoverageReady(CoverageReport) and CoverageError(String) variants for on-demand coverage"
  - "AppState with graph_metrics_report, clippy_report, coverage_report, coverage_running, community_highlight fields"
  - "RenderContext carrying graph_metrics_report, clippy_report, coverage_report references"
  - "file_color() dispatches ColorMode::Coverage to color_by_coverage() and ColorMode::Risk to color_by_risk()"
  - "color_by_coverage() falls back to monochrome when report is None; muted gray for uninstrumented files"
  - "color_by_risk() combines PageRank + coverage + clippy into risk_color() call"

affects:
  - 02.1-03
  - 02.1-04

tech-stack:
  added: []
  patterns:
    - "ScanReports struct extension: add fields for each new analysis; keep test constructors in sync"
    - "AppState clear_stale_state() must reset new fields to prevent stale cross-scan data"
    - "RenderContext is constructed in paint_render_frame() — add as_ref() fields from AppState"
    - "color_by_*() helper functions: and_then chain returns Option, unwrap_or provides fallback"

key-files:
  created: []
  modified:
    - sentrux-core/src/app/channels.rs
    - sentrux-core/src/app/scan_threads.rs
    - sentrux-core/src/app/state.rs
    - sentrux-core/src/app/scanning.rs
    - sentrux-core/src/app/update_loop.rs
    - sentrux-core/src/renderer/mod.rs
    - sentrux-core/src/renderer/rects.rs
    - sentrux-core/src/app/scanning_tests.rs

key-decisions:
  - "coverage_running flag and community_highlight field added to AppState for future Plan 03 toolbar wiring"
  - "color_by_risk() uses max_raw=1.0 as placeholder; Plan 03 can refine with project-level PageRank max"
  - "color_by_coverage() returns Color32::from_rgb(80, 80, 80) for uninstrumented files (not the theme's file_surface)"
  - "CoverageReady/CoverageError carry no generation counter — coverage is independent of scan generation"

patterns-established:
  - "Pattern: new ScanMsg variants require update to scanning_tests.rs match arm (non-exhaustive)"
  - "Pattern: AppState clear_stale_state() clears all analysis reports on directory change"
  - "Pattern: RenderContext fields mirror AppState analysis fields as Option<&'a T> references"

requirements-completed: [RANK-01, CLIP-01, COV-01, COV-03, RANK-03]

duration: 3min
completed: 2026-03-15
---

# Phase 02.1 Plan 02: Rust Deep Analysis - Pipeline Wiring Summary

**Complete data pipeline: graph-metrics and clippy flow from scan thread to AppState; Coverage and Risk ColorModes dispatch to real per-file color functions via RenderContext**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-15T04:55:44Z
- **Completed:** 2026-03-15T04:59:17Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Extended ScanReports with `graph_metrics: Option<GraphMetricsReport>` and `clippy: Option<ClippyReport>` fields; scan thread calls both new adapters and populates them
- Added `CoverageReady(CoverageReport)` and `CoverageError(String)` ScanMsg variants with handlers in poll_scan_messages
- Added 5 new fields to AppState: `graph_metrics_report`, `clippy_report`, `coverage_report`, `coverage_running`, `community_highlight`; all initialized to None/false and reset in clear_stale_state
- Extended RenderContext with `graph_metrics_report`, `clippy_report`, `coverage_report` fields populated from AppState in paint_render_frame
- Replaced Coverage/Risk monochrome fallback in file_color() with real `color_by_coverage()` and `color_by_risk()` helper functions
- `color_by_coverage()`: looks up line coverage pct via by_path index, returns coverage_color(); falls back to theme surface if no report; muted gray for uninstrumented files
- `color_by_risk()`: extracts basename for PageRank lookup, combines with coverage pct and clippy count via risk_color(); all data sources degrade gracefully when None

## Task Commits

1. **Task 1: Extend channels, scan pipeline, and AppState with new report fields** - `c3dae78` (feat)
2. **Task 2: Extend RenderContext and file_color() with Coverage and Risk dispatch** - `f0fe5b0` (feat)

## Files Created/Modified

- `sentrux-core/src/app/channels.rs` - Added GraphMetricsReport/CoverageReport/ClippyReport imports; graph_metrics/clippy to ScanReports; CoverageReady/CoverageError to ScanMsg
- `sentrux-core/src/app/scan_threads.rs` - Added run_graph_metrics/run_clippy_warnings imports and calls in send_scan_result
- `sentrux-core/src/app/state.rs` - Added GraphMetricsReport/CoverageReport/ClippyReport imports; 5 new fields to AppState struct and constructor
- `sentrux-core/src/app/scanning.rs` - apply_scan_reports stores new reports and resets coverage; clear_stale_state resets new fields; CoverageReady/CoverageError handlers added to poll_scan_messages
- `sentrux-core/src/app/update_loop.rs` - RenderContext construction in paint_render_frame now populates 3 new report fields
- `sentrux-core/src/renderer/mod.rs` - Added pmat_types imports; graph_metrics_report/clippy_report/coverage_report fields added to RenderContext
- `sentrux-core/src/renderer/rects.rs` - file_color() dispatch updated; color_by_coverage() and color_by_risk() implemented
- `sentrux-core/src/app/scanning_tests.rs` - Non-exhaustive match fix for new CoverageReady/CoverageError variants

## Decisions Made

- coverage_running and community_highlight added to AppState now to avoid a breaking struct change in Plan 03
- color_by_risk() uses `max_raw = 1.0` placeholder; Plan 03 can refine by computing project-level PageRank max during panel display
- Uninstrumented files in Coverage mode get `Color32::from_rgb(80, 80, 80)` — visually distinct from "no data" (monochrome) and "0% coverage" (red)
- CoverageReady/CoverageError carry no generation counter (coverage runs are independent, initiated by user action, not scan generation)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Closure variable capture error in color_by_risk() PageRank lookup**
- **Found during:** Task 2 (build verification)
- **Issue:** Used `r.data.nodes[idx].pagerank` outside the `and_then` closure — variable `r` not in scope at that point
- **Fix:** Collapsed into single `and_then(|gm| gm.by_filename.get(basename).map(|&idx| gm.data.nodes[idx].pagerank))` chain; same fix applied to coverage lookup
- **Files modified:** sentrux-core/src/renderer/rects.rs
- **Commit:** f0fe5b0 (Task 2 commit)

**2. [Rule 3 - Blocking] Non-exhaustive match in scanning_tests.rs for new ScanMsg variants**
- **Found during:** Task 2 test run
- **Issue:** scanning_tests.rs has a match on ScanMsg that became non-exhaustive after adding CoverageReady/CoverageError variants
- **Fix:** Added `Ok(ScanMsg::CoverageReady(_)) | Ok(ScanMsg::CoverageError(_)) => continue,` arm
- **Files modified:** sentrux-core/src/app/scanning_tests.rs
- **Commit:** f0fe5b0 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (Rule 1 - bug in closure, Rule 3 - non-exhaustive match)
**Impact on plan:** Both were minor correctness fixes caught during build/test. No scope changes.

## Issues Encountered

None — all builds succeeded after auto-fixes. 202 tests pass; 27 pre-existing oracle failures unchanged.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Complete data pipeline is now wired: scan → channel → AppState → RenderContext → file_color()
- Coverage is ready to be triggered on-demand via Plan 03 toolbar button (CoverageReady/CoverageError handlers in place)
- Risk color mode is fully functional with all three data sources (PageRank + coverage + clippy)
- coverage_running flag is available for Plan 03 to show progress indicator during coverage run
- community_highlight field is ready for Plan 04 BFS community visualization

## Self-Check: PASSED

All key files exist and both task commits are present in git history.

---
*Phase: 02.1-rust-deep-analysis*
*Completed: 2026-03-15*
