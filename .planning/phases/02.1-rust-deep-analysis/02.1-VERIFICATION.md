---
phase: 02.1-rust-deep-analysis
verified: 2026-03-15T05:38:56Z
status: passed
score: 5/5 success criteria verified
re_verification: false
human_verification:
  - test: "Community assignment display in file detail panel"
    expected: "Detail panel shows a 'Community' field or 'N connected files' for selected file in Risk mode"
    why_human: "pmat does not return per-node community IDs; RANK-02 says 'community assignment' but implementation uses treemap BFS highlight instead of panel text. User already confirmed Risk mode community highlight works on treemap — this item is whether the panel text label is also expected."
---

# Phase 02.1: Rust Deep Analysis Verification Report

**Phase Goal:** Sentrux provides deep Rust-specific analysis beyond TDG grades — PMAT code rank (PageRank, centrality, community detection), test coverage via cargo-llvm-cov, and clippy advanced linting — all visualized on the treemap and accessible in detail panels

**Verified:** 2026-03-15T05:38:56Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | PMAT code rank data (PageRank scores, centrality measures) is displayed per-file in the detail panel | VERIFIED | `draw_rank_section()` in pmat_panel.rs:58-100 shows PageRank, degree centrality, betweenness, in/out degree. Data flows from `state.graph_metrics_report` via `by_filename` index |
| 2 | A new ColorMode variant shows files colored by architectural importance (PageRank rank) | VERIFIED | `ColorMode::Risk` in layout/types.rs:27 dispatches to `color_by_risk()` in rects.rs:380; uses PageRank + coverage + clippy; `max_risk_raw` normalized per-frame via `compute_max_risk_raw()`; user confirmed correct gradient (commit d88c306) |
| 3 | Test coverage percentages from cargo-llvm-cov are shown per-file in the detail panel | VERIFIED | `draw_coverage_section()` in pmat_panel.rs:103-132 shows Lines% and Functions%. Coverage runs on-demand via `draw_coverage_button()` in toolbar.rs. User confirmed coverage button works |
| 4 | A new ColorMode variant shows files colored by test coverage (green = well-covered, red = uncovered) | VERIFIED | `ColorMode::Coverage` in layout/types.rs:24 dispatches to `color_by_coverage()` in rects.rs:363; uses `coverage_color()` from colors.rs:83; gated in toolbar until coverage_report is Some |
| 5 | Clippy warnings (pedantic/nursery categories) are counted per-file and shown in the detail panel | VERIFIED | `draw_clippy_section()` in pmat_panel.rs:135-157 shows total count + by-category breakdown. `run_clippy_warnings()` runs during scan, populates `state.clippy_report` |

**Score:** 5/5 success criteria verified

---

## Required Artifacts

### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `sentrux-core/src/core/pmat_types.rs` | GraphMetrics*, Coverage*, Clippy* types | VERIFIED | 3 report types confirmed present (grep count=3). `GraphMetricsNode` has 7 fields including pagerank/centrality. `lint_category()` maps 63 lint IDs |
| `sentrux-core/src/analysis/pmat_adapter.rs` | run_graph_metrics(), run_coverage(), run_clippy_warnings() | VERIFIED | All 3 functions present (grep count=3). `check_llvm_cov_available()` also confirmed |
| `sentrux-core/src/renderer/colors.rs` | coverage_color(), risk_color() | VERIFIED | Both functions at lines 83 and 99. Gradient tests pass (6 color tests all green) |
| `sentrux-core/src/layout/types.rs` | ColorMode::Coverage and ColorMode::Risk before Monochrome | VERIFIED | Coverage at line 24, Risk at line 27, Monochrome at line 33 — correct ordering preserved |

### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `sentrux-core/src/app/channels.rs` | ScanReports with graph_metrics/clippy, ScanMsg::CoverageReady | VERIFIED | `CoverageReady` at line 76, `CoverageError` at line 78 confirmed |
| `sentrux-core/src/app/scan_threads.rs` | run_graph_metrics and run_clippy_warnings called during scan | VERIFIED | Both imported and called at lines 146 and 149 |
| `sentrux-core/src/app/state.rs` | graph_metrics_report, clippy_report, coverage_report, coverage_running fields | VERIFIED | All 4 fields at lines 161-167; initialized at lines 281-284 |
| `sentrux-core/src/renderer/rects.rs` | ColorMode::Coverage and ColorMode::Risk match arms in file_color() | VERIFIED | Lines 355-356; `color_by_coverage()` at 363 and `color_by_risk()` at 380 both substantive |

### Plan 03 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `sentrux-core/src/app/panels/pmat_panel.rs` | Coverage, clippy, and rank sections | VERIFIED | `draw_rank_section` at 58, `draw_coverage_section` at 103, `draw_clippy_section` at 135 — all substantive implementations with fallback states |
| `sentrux-core/src/app/toolbar.rs` | Run Coverage button and gated Coverage ColorMode | VERIFIED | `draw_coverage_button()` at 207 sets `coverage_requested` flag; Coverage ColorMode disabled when `coverage_report.is_none()` at line 170 |

---

## Key Link Verification

### Plan 01 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `pmat_adapter.rs` | `pmat_types.rs` | subprocess output deserialized into report types | VERIFIED | `run_graph_metrics` returns `Option<GraphMetricsReport>` via `GraphMetricsReport::from_output()`; `run_coverage` returns `Option<CoverageReport>` |
| `renderer/colors.rs` | `layout/types.rs` | color functions used by ColorMode dispatch | VERIFIED | `coverage_color()` and `risk_color()` called from `color_by_coverage()` and `color_by_risk()` in rects.rs, which dispatches from `file_color()` match on ColorMode |

### Plan 02 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `scan_threads.rs` | `channels.rs` | ScanReports struct with graph_metrics and clippy fields | VERIFIED | `run_graph_metrics` and `run_clippy_warnings` called; results stored in `ScanReports{graph_metrics, clippy, ...}` at lines 146-152 |
| `scanning.rs` | `state.rs` | ScanMsg::Complete handler stores reports on AppState | VERIFIED | `apply_scan_reports()` at lines 98-101 sets `state.graph_metrics_report`, `state.clippy_report`, resets `coverage_report`; `CoverageReady` handler at line 58 sets `coverage_report` |
| `renderer/rects.rs` | `renderer/colors.rs` | file_color() calls risk_color() and coverage_color() | VERIFIED | `color_by_coverage()` calls `coverage_color(pct)` and `color_by_risk()` calls `colors::risk_color(...)` at line 397 |

### Plan 03 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `pmat_panel.rs` | `state.rs` | reads graph_metrics_report, coverage_report, clippy_report from AppState | VERIFIED | All three fields accessed via `state.graph_metrics_report`, `state.coverage_report`, `state.clippy_report` in respective draw functions |
| `toolbar.rs` | `channels.rs` | spawns thread that sends ScanMsg::CoverageReady | VERIFIED | toolbar sets `state.coverage_requested = true`; `draw_panels.rs:40-63` catches flag and calls `maybe_spawn_coverage_thread()` which sends `ScanMsg::CoverageReady(report)` |

---

## Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| RANK-01 | 01, 02 | PMAT code rank data computed via `pmat analyze graph-metrics` subprocess | SATISFIED | `run_graph_metrics()` in pmat_adapter.rs calls `pmat analyze graph-metrics --format json --path`; report flows through scan pipeline to AppState |
| RANK-02 | 01, 02, 03 | File detail panel shows PageRank score, centrality measures, and community assignment | SATISFIED (partial note) | Panel shows PageRank, degree centrality, betweenness, in/out degree. "Community assignment" is not a panel label — pmat provides no per-node community IDs (per RESEARCH.md). Implemented as BFS treemap highlight (click file in Risk mode). User confirmed interaction works. |
| RANK-03 | 01, 02 | ColorMode variant colors treemap by architectural importance | SATISFIED | `ColorMode::Risk` combines PageRank + coverage + clippy; normalization fixed in d88c306; user confirmed correct gradient display |
| COV-01 | 01, 02 | Coverage collected via `cargo llvm-cov` subprocess | SATISFIED | `run_coverage()` calls `cargo llvm-cov --json --summary-only --ignore-run-fail`; user confirmed button works |
| COV-02 | 01, 02, 03 | File detail panel shows line coverage percentage | SATISFIED | `draw_coverage_section()` shows Lines% and Functions%; user confirmed display correct |
| COV-03 | 01, 02, 03 | ColorMode variant colors treemap by test coverage | SATISFIED | `ColorMode::Coverage` dispatches to `color_by_coverage()`; gated until data available; user confirmed colors display correctly |
| CLIP-01 | 01, 02 | Clippy warnings collected via `cargo clippy` subprocess | SATISFIED | `run_clippy_warnings()` calls `cargo clippy --message-format=json -- -W clippy::pedantic -W clippy::nursery`; runs during scan |
| CLIP-02 | 01, 02, 03 | File detail panel shows clippy warning count and categories | SATISFIED | `draw_clippy_section()` shows total count + category breakdown; `lint_category()` maps 63 lint IDs to correctness/performance/complexity/style |

All 8 requirement IDs from PLANs cross-referenced — no orphaned requirements found.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `sentrux-core/src/app/panels/pmat_panel.rs` | ~318 | `id_source("pmat_penalties")` deprecated API (build warning) | Info | Non-blocking — 4 warnings total during build, all pre-existing |

No TODO/FIXME/placeholder patterns found in phase-modified files. No empty implementations. No stub return values. Build produces 4 warnings, all pre-existing (build output: `warning: 'sentrux-core' (lib) generated 4 warnings`).

---

## Test Results

- **Phase 2.1 specific tests:** 26 passed, 0 failed (filtered run targeting graph_metrics, coverage, clippy, color_mode, risk)
- **Full test suite:** 202 passed, 27 failed — the 27 failures are all pre-existing oracle/parser tests for language modules (bash, go, python, css, etc.) confirmed pre-existing in SUMMARY.md notes from all three plans. Zero Phase 2.1 tests in the failure list.
- **Build:** Clean — `Finished 'dev' profile` with 4 pre-existing warnings

---

## Human Verification Notes

One item where user confirmation has already been provided:

1. **Coverage button and colors** — User confirmed: coverage button works, coverage colors display correctly.
2. **Risk mode normalization** — User confirmed: after d88c306 fix, Risk mode shows correct color gradient (riskiest file is red).
3. **Community highlight** — Community BFS highlight on treemap implemented and wired. User tested Risk mode; community highlight was part of that verification session.

One soft note on RANK-02 "community assignment":
- REQUIREMENTS.md says "community assignment for selected file" as a panel display item
- pmat does not provide per-node community IDs (RESEARCH.md explicitly notes this limitation)
- Implementation: BFS community highlight on treemap (click file in Risk mode = connected imports highlight)
- The panel does NOT show a "Community: X" text label
- This is the only feasible approach given the data available
- User confirmed the treemap interaction works

If the product owner considers "community assignment" to require a panel text label (e.g., "Community: 12 connected files"), that is a scope item for a follow-up. For the purposes of this verification, the implementation satisfies the intent of RANK-02 given the technical constraints documented in RESEARCH.md.

---

## Gaps Summary

No blocking gaps. All five ROADMAP success criteria are verified. All eight requirements are satisfied. The pipeline is complete from subprocess invocation through AppState through RenderContext to treemap color dispatch and file detail panel display.

The single nuance (RANK-02 community assignment as panel text vs. treemap interaction) is a scope interpretation, not an implementation defect, and is documented above for product owner awareness.

---

_Verified: 2026-03-15T05:38:56Z_
_Verifier: Claude (gsd-verifier)_
