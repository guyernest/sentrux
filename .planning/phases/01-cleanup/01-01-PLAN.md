---
phase: 01-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentrux-bin/src/main.rs
  - sentrux-core/src/app/mod.rs
  - sentrux-core/src/app/mcp_server/
  - sentrux-core/src/analysis/mod.rs
  - sentrux-core/src/analysis/plugin/
  - sentrux-core/src/metrics/mod.rs
  - sentrux-core/src/metrics/whatif/
  - sentrux-core/src/app/panels/whatif_display.rs
  - sentrux-core/src/app/panels/mod.rs
  - sentrux-core/src/app/draw_panels.rs
  - sentrux-core/src/app/state.rs
  - sentrux-core/Cargo.toml
autonomous: true
requirements:
  - CLEN-01
  - CLEN-02

must_haves:
  truths:
    - "Binary builds without MCP server code; `sentrux mcp` is not a valid subcommand"
    - "No plugin loading executes at startup; no access to ~/.sentrux/plugins/"
    - "`sentrux plugin` is not a valid subcommand"
    - "whatif module and whatif_display panel are removed; no dead-code warnings from them"
    - "`pub use evo as evolution` alias is removed (only consumer was MCP server)"
    - "Existing CLI commands (check, gate) and GUI mode still work"
  artifacts:
    - path: "sentrux-bin/src/main.rs"
      provides: "CLI entry point without MCP, plugin, or auto-install code"
    - path: "sentrux-core/src/app/mod.rs"
      provides: "App module without mcp_server declaration"
    - path: "sentrux-core/src/analysis/mod.rs"
      provides: "Analysis module without plugin declaration"
    - path: "sentrux-core/src/metrics/mod.rs"
      provides: "Metrics module without whatif or evolution alias"
  key_links:
    - from: "sentrux-bin/src/main.rs"
      to: "sentrux-core::app"
      via: "run_gui, run_check, run_gate still work"
      pattern: "run_gui|run_check|run_gate"
    - from: "sentrux-core/src/metrics/mod.rs"
      to: "sentrux-core/src/metrics/grading.rs"
      via: "compute_health still compiles"
      pattern: "compute_health"
---

<objective>
Remove the MCP server, plugin system, and partial metrics dead code from the sentrux codebase.

Purpose: Strip out subsystems that will not carry forward into the PMAT-powered product. The MCP server is replaced by PMAT's own MCP. The plugin system is replaced by compiled-in grammars (Plan 02). The whatif module and evolution alias are only used by MCP handlers being deleted. Note: whatif/evolution removal is preparatory cleanup — the full metrics engine removal (CLEN-04) is Phase 2 work after PMAT replaces it.

Output: A binary that builds cleanly without MCP, plugin, or whatif code. CLI check/gate and GUI mode remain fully functional.
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleanup/01-RESEARCH.md
@.planning/phases/01-cleanup/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove MCP server and plugin system from binary and core crates</name>
  <files>
    sentrux-bin/src/main.rs
    sentrux-core/src/app/mod.rs
    sentrux-core/src/app/mcp_server/ (delete entire directory)
    sentrux-core/src/analysis/mod.rs
    sentrux-core/src/analysis/plugin/ (delete entire directory)
  </files>
  <action>
    This is a pure deletion task. Work in this exact order to avoid intermediate compile errors:

    **Step 0: Check for sentrux-pro workspace member.**
    Run `grep -n "sentrux-pro\|members" sentrux-core/../Cargo.toml` (the root workspace Cargo.toml) to verify whether a `sentrux-pro` private crate exists as a workspace member. If it does, note its dependencies on MCP/plugin code — deletions may need to account for it. If it does not exist, proceed normally.

    1. **Delete the MCP server module directory:** `rm -rf sentrux-core/src/app/mcp_server/`
    2. **Delete the plugin loader module directory:** `rm -rf sentrux-core/src/analysis/plugin/`
    3. **Edit `sentrux-core/src/app/mod.rs`:** Remove the line `pub mod mcp_server;`
    4. **Edit `sentrux-core/src/analysis/mod.rs`:** Remove the line `pub mod plugin;`
    5. **Edit `sentrux-bin/src/main.rs` — remove ALL of the following:**
       - The `#[arg(long = "mcp", hide = true)] mcp_flag: bool` field on the `Cli` struct (lines 44-46 area)
       - The `if cli.mcp_flag { ... }` dispatch block (lines 132-135 area)
       - The `Command::Mcp` variant from the `Command` enum (lines 75-76 area)
       - The `Some(Command::Mcp) => { ... }` match arm (lines 144-147 area)
       - The entire `PluginAction` enum definition
       - The `Command::Plugin { action: PluginAction }` variant from the `Command` enum
       - The `Some(Command::Plugin { action }) => { ... }` match arm
       - The entire `run_plugin()` function (lines 338-546 area)
       - The `auto_install_plugins_if_needed()` call in `main()` (line 124 area)
       - The entire `auto_install_plugins_if_needed()` function definition (lines 672-737 area)
       - All `use` statements referencing `plugin`, `mcp_server`, or `PluginAction`
    6. **Run `cargo check --workspace`** to find any remaining dangling references. Fix any `use` statements or type references that still point to deleted modules.

    What stays in `main.rs`: `run_gui`, `run_check`, `run_gate`, `cli_scan_limits`, GPU probing, `version_string`, `Command::Scan`, `Command::Check`, `Command::Gate`.

    **WARNING:** Do NOT delete `sentrux-core/src/metrics/` anything in this task — that is Task 2. Do NOT touch `lang_registry.rs` — that is Plan 02.
  </action>
  <verify>
    <automated>cd /Users/guy/projects/sentrux/sentrux && cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `sentrux-core/src/app/mcp_server/` directory does not exist
    - `sentrux-core/src/analysis/plugin/` directory does not exist
    - `cargo check --workspace` succeeds with no errors
    - No references to `mcp_server`, `plugin`, `PluginAction`, or `auto_install_plugins` remain in `main.rs`
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove whatif module, whatif_display panel, evolution alias, and clean up dependencies</name>
  <files>
    sentrux-core/src/metrics/mod.rs
    sentrux-core/src/metrics/whatif/ (delete entire directory)
    sentrux-core/src/app/panels/whatif_display.rs (delete file)
    sentrux-core/src/app/panels/mod.rs
    sentrux-core/src/app/draw_panels.rs
    sentrux-core/src/app/state.rs
    sentrux-core/Cargo.toml
  </files>
  <action>
    This task removes the whatif module (only consumed by MCP handlers and whatif_display panel, both now gone) and the `pub use evo as evolution` alias (only consumed by `mcp_handlers_evo.rs`, now gone). This is preparatory cleanup — no requirement claim on CLEN-04 (full metrics engine removal is Phase 2).

    1. **Delete whatif module:** `rm -rf sentrux-core/src/metrics/whatif/`
    2. **Delete whatif display panel:** `rm sentrux-core/src/app/panels/whatif_display.rs`
    3. **Edit `sentrux-core/src/metrics/mod.rs`:**
       - Remove the line `pub mod whatif;`
       - Remove the line `pub use evo as evolution;`
    4. **Edit `sentrux-core/src/app/panels/mod.rs`:** Remove the `pub mod whatif_display;` line (or equivalent reference).
    5. **Edit `sentrux-core/src/app/draw_panels.rs`:** Remove any call to `whatif_display::*` or references to the whatif panel. Look for panel drawing calls and remove the whatif one.
    6. **Edit `sentrux-core/src/app/state.rs`:** If there is a `show_whatif_panel` or similar bool, remove it.
    7. **Remove unused dependencies from `sentrux-core/Cargo.toml`:**
       - Run `cargo remove libloading dirs --manifest-path sentrux-core/Cargo.toml` (these were only used by the plugin loader deleted in Task 1)
    8. **Run `cargo check --workspace`** to verify clean compilation.
    9. **Run `cargo test -p sentrux-core 2>&1 | tail -20`** to verify existing tests still pass.

    **IMPORTANT:** Do NOT remove `grading.rs`, `stability.rs`, `arch/`, `evo/`, `dsm/`, `testgap/`, or `rules/` from metrics — those are actively used by `sentrux check` and `sentrux gate` and will be removed in Phase 2 after PMAT replaces them.
  </action>
  <verify>
    <automated>cd /Users/guy/projects/sentrux/sentrux && cargo test -p sentrux-core 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `sentrux-core/src/metrics/whatif/` directory does not exist
    - `sentrux-core/src/app/panels/whatif_display.rs` does not exist
    - `pub use evo as evolution` line is gone from `metrics/mod.rs`
    - `libloading` and `dirs` are no longer in `sentrux-core/Cargo.toml`
    - `cargo test -p sentrux-core` passes
    - `sentrux check` and `sentrux gate` CLI commands still work (metrics engine intact)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cargo build --workspace` succeeds with zero errors
2. `cargo test -p sentrux-core` passes all existing tests
3. `grep -r "mcp_server\|pub mod plugin\|auto_install_plugins\|PluginAction\|whatif\|pub use evo as evolution" sentrux-bin/src/ sentrux-core/src/ --include="*.rs"` returns no matches (except comments if any)
4. Binary size should decrease (MCP + plugin + whatif code removed)
</verification>

<success_criteria>
- MCP server code is completely removed from the codebase
- Plugin loading system is completely removed from the codebase
- whatif module and panel are removed
- evolution alias is removed
- libloading and dirs dependencies are removed from Cargo.toml
- All existing tests pass
- CLI check/gate and GUI mode remain functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-01-SUMMARY.md`
</output>
