---
phase: 01-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - sentrux-core/src/analysis/lang_registry.rs
  - sentrux-core/Cargo.toml
  - sentrux-core/src/queries/ (delete all except rust/, typescript/, javascript/)
autonomous: true
requirements:
  - CLEN-03

must_haves:
  truths:
    - "Sentrux correctly scans Rust (.rs) files with structural parsing"
    - "Sentrux correctly scans TypeScript (.ts, .tsx) files with structural parsing"
    - "Sentrux correctly scans JavaScript (.js, .jsx, .mjs, .cjs) files with structural parsing"
    - "Sentrux silently skips all other file types (e.g., .py, .go, .java) without error or panic"
    - "No runtime plugin loading occurs — grammars are compiled into the binary"
    - "lang_registry unit tests pass for supported and unsupported extensions"
  artifacts:
    - path: "sentrux-core/src/analysis/lang_registry.rs"
      provides: "Static 3-language registry with compiled-in grammars"
      contains: "tree_sitter_rust"
    - path: "sentrux-core/src/queries/rust/tags.scm"
      provides: "Rust tree-sitter query file"
    - path: "sentrux-core/src/queries/typescript/tags.scm"
      provides: "TypeScript tree-sitter query file"
    - path: "sentrux-core/src/queries/javascript/tags.scm"
      provides: "JavaScript tree-sitter query file"
  key_links:
    - from: "sentrux-core/src/analysis/lang_registry.rs"
      to: "tree-sitter-rust crate"
      via: "tree_sitter_rust::LANGUAGE"
      pattern: "tree_sitter_rust"
    - from: "sentrux-core/src/analysis/lang_registry.rs"
      to: "sentrux-core/src/queries/rust/tags.scm"
      via: "include_str!"
      pattern: "include_str!"
    - from: "sentrux-core/src/analysis/parser/"
      to: "sentrux-core/src/analysis/lang_registry.rs"
      via: "get_grammar_and_query or equivalent public API"
      pattern: "lang_registry::"
---

<objective>
Rewrite the language registry as a static 3-language registry with compiled-in tree-sitter grammars for Rust, TypeScript, and JavaScript. Delete all unused language query files.

Purpose: After Plan 01 removed the plugin system, the `LangRegistry` has no working language support. This plan replaces the plugin-based registry with a static registry that compiles grammars directly into the binary, narrowing language support to only Rust/TypeScript/JavaScript as specified by CLEN-03.

Output: A fully functional static `lang_registry.rs` with compiled-in grammars. The binary can parse Rust, TypeScript, and JavaScript files. All other file types are silently skipped.
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleanup/01-RESEARCH.md
@.planning/phases/01-cleanup/01-VALIDATION.md
@.planning/phases/01-cleanup/01-01-SUMMARY.md

<interfaces>
<!-- Key API from lang_registry.rs that the parser and scanner depend on. -->
<!-- The executor MUST preserve these public function signatures (or their equivalents) -->
<!-- because parser/ and scanner/ modules call them. -->

Current public API (from lang_registry.rs — all must be preserved or replaced with equivalent):
- `get_grammar_and_query(name: &str) -> Option<(&Language, &Query)>` — parser calls this
- `detect_lang_from_ext(ext: &str) -> &str` — scanner calls this to map file extensions to language names
- `get_by_ext(ext: &str) -> Option<&PluginLangConfig>` — scanner may call this
- `all_extensions() -> Vec<String>` — may be used for scan filtering

The executor must grep for all `lang_registry::` usages across the codebase to discover the exact API surface before rewriting.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grammar crates, rewrite lang_registry.rs as static registry, update tests</name>
  <files>
    sentrux-core/Cargo.toml
    sentrux-core/src/analysis/lang_registry.rs
  </files>
  <behavior>
    - Test: `get_grammar_and_query("rust")` returns Some (valid Language + Query)
    - Test: `get_grammar_and_query("typescript")` returns Some
    - Test: `get_grammar_and_query("javascript")` returns Some
    - Test: `get_grammar_and_query("python")` returns None
    - Test: `get_grammar_and_query("go")` returns None
    - Test: `detect_lang_from_ext("rs")` returns "rust"
    - Test: `detect_lang_from_ext("ts")` returns "typescript"
    - Test: `detect_lang_from_ext("tsx")` returns "typescript"
    - Test: `detect_lang_from_ext("js")` returns "javascript"
    - Test: `detect_lang_from_ext("jsx")` returns "javascript"
    - Test: `detect_lang_from_ext("mjs")` returns "javascript"
    - Test: `detect_lang_from_ext("cjs")` returns "javascript"
    - Test: `detect_lang_from_ext("py")` returns "unknown" (or similar non-parseable indicator)
    - Test: `detect_lang_from_ext("go")` returns "unknown"
  </behavior>
  <action>
    **Step 0: Discover exact public API surface.**
    Run `grep -rn "lang_registry::" sentrux-core/src/ --include="*.rs"` to find every call site. Document all functions that must be preserved. Check the existing test module in `lang_registry.rs` to understand what tests exist.

    **Step 1: Add grammar crate dependencies.**
    ```bash
    cargo add tree-sitter-rust tree-sitter-typescript tree-sitter-javascript --manifest-path sentrux-core/Cargo.toml
    ```
    Then run `cargo check -p sentrux-core` to verify the crates resolve and their `LANGUAGE` constant API shape. The grammar crates may export `LANGUAGE` as a `LanguageFn` that needs `.into()` to become `Language`, or they may use the newer `tree_sitter::Language::new()` pattern. Check actual exports before writing registry code.

    **Step 2: Rewrite `lang_registry.rs` as a complete static registry.**
    Replace the entire file. The new implementation must:
    - Use `LazyLock` (or `OnceLock`) for three static `LangConfig` entries (Rust, TypeScript, JavaScript)
    - Load grammars from the crate constants (e.g., `tree_sitter_rust::LANGUAGE`)
    - Load queries via `include_str!("../queries/rust/tags.scm")` (verify path relative to file)
    - Compile `Query::new(&grammar, query_source)` at init time
    - Preserve ALL public functions discovered in Step 0 with compatible signatures
    - Keep the extension-to-display-name fallback table for non-parseable types (json, toml, yaml, etc.) IF the scanner or other callers use it. These return a name but `get_grammar_and_query` returns None for them.
    - Remove all `PluginLangConfig`, `LangRegistry` struct, `load_plugins()`, and any plugin-related code

    Use the research document's Pattern 1 as a starting reference, but verify against actual API shape discovered in Step 0.

    **Step 3: Update/write unit tests** per the `<behavior>` block above. Place tests in a `#[cfg(test)] mod tests` block at the bottom of `lang_registry.rs`.

    **Step 4: Run `cargo check --workspace`** to verify the parser and scanner still compile against the new API. If any call site breaks, fix the call site to match the new API. The goal is API-compatible replacement — no changes should be needed in parser/ or scanner/ if the public function signatures are preserved.
  </action>
  <verify>
    <automated>cd /Users/guy/projects/sentrux/sentrux && cargo test -p sentrux-core lang_registry 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `lang_registry.rs` is a static registry with no plugin loading code
    - `tree-sitter-rust`, `tree-sitter-typescript`, `tree-sitter-javascript` are in Cargo.toml
    - All tests in `<behavior>` block pass
    - `cargo check --workspace` succeeds (parser/scanner still compile)
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete unused language query directories, add integration test for unknown extensions, verify full build</name>
  <files>
    sentrux-core/src/queries/ (delete all except rust/, typescript/, javascript/)
  </files>
  <action>
    **Step 0: Confirm query directories exist before deletion.**
    Run `ls sentrux-core/src/queries/` to see all directories. Verify that the directories to be deleted actually exist. If any listed directory does not exist, skip its deletion (do not fail).

    1. **Delete all query directories except the three kept languages:**
       ```bash
       cd sentrux-core/src/queries/
       # Keep: rust/, typescript/, javascript/
       # Delete everything else:
       rm -rf bash/ c/ cpp/ csharp/ css/ dockerfile/ elixir/ go/ haskell/ html/ java/ kotlin/ lua/ ocaml/ php/ python/ r/ ruby/ scala/ scss/ swift/ zig/
       ```

    2. **Verify no `include_str!` references point to deleted files:**
       Run `grep -rn "include_str!" sentrux-core/src/ --include="*.rs"` and confirm every path resolves to an existing file.

    3. **Add integration test for unknown extension handling.**
       Create a test (in `sentrux-core/tests/` or as an integration test in the appropriate module) that:
       - Creates a temp directory with a `.py` file and a `.go` file (minimal content)
       - Runs the scanner/analysis pipeline on that directory
       - Asserts no panic occurs and no error is returned
       - Verifies the files are silently skipped (not included in parsed results)
       This satisfies the Wave 0 requirement from VALIDATION.md: "scan directory with .py/.go files without panic."

    4. **Run full workspace build and tests:**
       ```bash
       cargo build --workspace
       cargo test --workspace
       ```

    5. **Verify no dead-code warnings from removed subsystems:**
       ```bash
       cargo build --workspace 2>&1 | grep -i "warning.*dead_code\|warning.*unused"
       ```
       Some pre-existing warnings may exist — only care about warnings related to MCP, plugin, whatif, or language registry code.
  </action>
  <verify>
    <automated>cd /Users/guy/projects/sentrux/sentrux && cargo test --workspace 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Only `rust/`, `typescript/`, `javascript/` directories remain under `sentrux-core/src/queries/`
    - `cargo build --workspace` succeeds
    - `cargo test --workspace` passes all tests
    - Integration test exists that scans .py/.go files without panic
    - No dead-code warnings from removed subsystems
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cargo build --release --workspace` succeeds with zero errors
2. `cargo test --workspace` passes all tests
3. `ls sentrux-core/src/queries/` shows only `rust/`, `typescript/`, `javascript/`
4. `grep -r "plugin\|load_plugins\|PluginLangConfig" sentrux-core/src/analysis/lang_registry.rs` returns no matches
5. Scanning a directory with mixed file types (Rust + Python + Go) completes without error — Rust files get structural analysis, others are silently skipped
</verification>

<success_criteria>
- Static 3-language registry replaces plugin-based registry
- Rust, TypeScript, and JavaScript files are correctly parsed with compiled-in grammars
- All other file types are silently skipped without error
- No plugin, query, or grammar files for unsupported languages remain
- Integration test proves no panic on unknown extensions
- All workspace tests pass
- Binary builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-02-SUMMARY.md`
</output>
