---
phase: 04-gsd-phase-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentrux-core/src/core/pmat_types.rs
  - sentrux-core/src/analysis/gsd_phase_adapter.rs
  - sentrux-core/src/analysis/mod.rs
  - sentrux-core/src/layout/types.rs
  - sentrux-core/src/renderer/colors.rs
  - sentrux-core/src/renderer/rects.rs
  - sentrux-core/src/renderer/mod.rs
  - sentrux-core/src/app/channels.rs
  - sentrux-core/src/app/scanning.rs
  - sentrux-core/src/app/scanning_tests.rs
  - sentrux-core/src/metrics/evo/git_walker.rs
autonomous: true
requirements:
  - GSDP-01
  - GSDP-03
  - GSDP-04

must_haves:
  truths:
    - "GSD phase data types compile and represent completed/in-progress/planned phases with file lists"
    - "ROADMAP.md is parsed into a list of PhaseInfo with correct status detection (checkbox-based)"
    - "PLAN.md files_modified and SUMMARY.md key-files are collected into per-phase file lists"
    - "ColorMode::GsdPhase variant exists and dispatches to color_by_gsd_phase() in file_color()"
    - "Three-state colors render: green for completed, amber for in-progress, blue for planned; muted gray for unassociated"
    - "DiffWindow::CommitRange variant exists for phase-based git diff navigation"
  artifacts:
    - path: "sentrux-core/src/core/pmat_types.rs"
      provides: "PhaseStatus, PhaseInfo, GsdPhaseReport types"
      contains: "pub enum PhaseStatus"
    - path: "sentrux-core/src/analysis/gsd_phase_adapter.rs"
      provides: "parse_gsd_phases(), ROADMAP/PLAN/SUMMARY parsing"
      exports: ["parse_gsd_phases"]
    - path: "sentrux-core/src/layout/types.rs"
      provides: "ColorMode::GsdPhase variant before Monochrome"
      contains: "GsdPhase"
    - path: "sentrux-core/src/renderer/colors.rs"
      provides: "gsd_phase_color() function"
      exports: ["gsd_phase_color"]
    - path: "sentrux-core/src/renderer/rects.rs"
      provides: "color_by_gsd_phase() dispatch arm"
      contains: "color_by_gsd_phase"
  key_links:
    - from: "sentrux-core/src/renderer/rects.rs"
      to: "sentrux-core/src/renderer/colors.rs"
      via: "color_by_gsd_phase() calls gsd_phase_color()"
      pattern: "gsd_phase_color"
    - from: "sentrux-core/src/analysis/gsd_phase_adapter.rs"
      to: "sentrux-core/src/core/pmat_types.rs"
      via: "constructs GsdPhaseReport from parsed data"
      pattern: "GsdPhaseReport"
---

<objective>
Create the GSD phase overlay data foundation: types, parser, ColorMode variant, color functions, ScanMsg variants, and DiffWindow::CommitRange.

Purpose: Establish all data types and the parsing adapter so that Plan 02 can wire the pipeline through AppState/RenderContext, and Plan 03 can build the UI.
Output: Compiling types + parser + color dispatch; no runtime wiring yet (RenderContext.gsd_phase_report is None placeholder).
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gsd-phase-overlay/04-CONTEXT.md
@.planning/phases/04-gsd-phase-overlay/04-RESEARCH.md
@.planning/phases/03-git-diff-overlay/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From sentrux-core/src/layout/types.rs (ColorMode enum — GsdPhase goes BEFORE Monochrome):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "lowercase")]
pub enum ColorMode {
    Language, Heat, Git, TdgGrade, Coverage, Risk,
    #[serde(rename = "GitDiff")]
    GitDiff,
    // GsdPhase goes HERE — before Monochrome
    #[serde(other)]
    Monochrome, // MUST remain last
}
```

From sentrux-core/src/renderer/rects.rs (file_color dispatch):
```rust
pub fn file_color(ctx: &RenderContext, path: &str) -> Color32 {
    match ctx.color_mode {
        ColorMode::Monochrome => ctx.theme_config.file_surface,
        ColorMode::Language => color_by_language(ctx, path),
        // ... other arms ...
        ColorMode::GitDiff => color_by_git_diff(ctx, path),
    }
}
```

From sentrux-core/src/renderer/mod.rs (RenderContext — add gsd_phase_report field):
```rust
pub struct RenderContext<'a> {
    // ... existing fields ...
    pub git_diff_report: Option<&'a GitDiffReport>,
    // gsd_phase_report goes here
}
```

From sentrux-core/src/app/channels.rs (ScanMsg — add GsdPhaseReady/GsdPhaseError):
```rust
pub enum ScanMsg {
    // ... existing variants ...
    GitDiffReady(crate::core::pmat_types::GitDiffReport),
    GitDiffError(String),
    // GsdPhaseReady/GsdPhaseError go here
}
```

From sentrux-core/src/metrics/evo/git_walker.rs (DiffWindow — add CommitRange):
```rust
pub enum DiffWindow {
    TimeSecs(i64),
    CommitCount(u32),
    SinceLastTag,
    // CommitRange { from: String, to: String } goes here
}
```

From sentrux-core/src/renderer/colors.rs:
```rust
pub const NO_DATA_GRAY: Color32 = Color32::from_rgb(50, 52, 55);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GSD phase types, parser adapter, and DiffWindow::CommitRange</name>
  <files>
    sentrux-core/src/core/pmat_types.rs,
    sentrux-core/src/analysis/gsd_phase_adapter.rs,
    sentrux-core/src/analysis/mod.rs,
    sentrux-core/src/metrics/evo/git_walker.rs,
    sentrux-core/src/app/channels.rs,
    sentrux-core/src/app/scanning.rs,
    sentrux-core/src/app/scanning_tests.rs
  </files>
  <behavior>
    - Test: parse_roadmap_phases() extracts phases from ROADMAP.md content with correct status (completed/in-progress/planned), names, goals, and completion dates
    - Test: first incomplete phase is InProgress, subsequent incompletes are Planned
    - Test: extract_files_modified() parses PLAN.md YAML frontmatter files_modified list
    - Test: extract_key_files() parses SUMMARY.md key-files.created + key-files.modified
    - Test: normalize_path() strips "./" prefix and trailing "/"
    - Test: zero_pad_phase() pads "1" to "01", "2.1" to "02.1", "4" to "04"
    - Test: GsdPhaseReport::phase_for_file() returns correct phase for exact match
    - Test: find_directory_match() matches directory prefix entries (e.g., "src/app/" matches "src/app/state.rs")
    - Test: parse_gsd_phases() returns None when no .planning/ directory exists
    - Test: DiffWindow::CommitRange serializes and deserializes correctly
  </behavior>
  <action>
    1. Add types to sentrux-core/src/core/pmat_types.rs (after GitDiffReport section):
       - `PhaseStatus` enum: Completed, InProgress, Planned (derive Debug, Clone, Copy, PartialEq, Eq)
       - `PhaseInfo` struct: number (String), name (String), goal (String), status (PhaseStatus), completed_date (Option<String>), files (Vec<String>), commit_range (Option<(String, String)>)
       - `GsdPhaseReport` struct: phases (Vec<PhaseInfo>), by_file (HashMap<String, usize>)
       - Impl GsdPhaseReport: phase_count() -> usize, phase_for_file(path) -> Option<&PhaseInfo>

    2. Create sentrux-core/src/analysis/gsd_phase_adapter.rs:
       - `parse_gsd_phases(scan_root: &str) -> Option<GsdPhaseReport>`: find .planning/ dir, parse ROADMAP.md, collect files from PLAN.md/SUMMARY.md, detect commit ranges, build by_file lookup (most recent phase wins per user decision)
       - `find_planning_dir(scan_root: &str) -> Option<PathBuf>`: check scan_root/.planning/, walk up 3 parents
       - `parse_roadmap_phases(content: &str) -> Vec<PhaseInfo>`: regex parse `- [x/] **Phase N: Name** - Goal (completed DATE)`. Status logic: [x]=Completed, first [ ]=InProgress, subsequent [ ]=Planned
       - `collect_phase_files(phases_dir, phase, scan_root)`: find phase dir by number (with zero_pad_phase), read *-PLAN.md files_modified and *-SUMMARY.md key-files, union into phase.files
       - `detect_phase_commit_ranges(scan_root, phases)`: git2 revwalk (capped 2000 commits, REVERSE sort), parse commit scopes `\((\d+)(?:[.-](\d+))?`, match to phases, set commit_range (first_oid, last_oid)
       - `extract_files_modified(plan_path) -> Option<Vec<String>>`: parse YAML frontmatter files_modified list
       - `extract_key_files(summary_path) -> Option<Vec<String>>`: parse key-files.created + key-files.modified
       - `extract_frontmatter(content) -> Option<&str>`: between first `---\n` and next `\n---`
       - `parse_yaml_string_list(content, key) -> Option<Vec<String>>`: manual YAML sequence parser
       - `parse_yaml_nested_list(content, parent_key, child_key) -> Option<Vec<String>>`: for key-files.created/modified
       - `normalize_path(path) -> String`: strip "./" prefix, strip trailing "/"
       - `zero_pad_phase(number) -> String`: "1" -> "01", "2.1" -> "02.1"
       - `find_directory_match(by_file, path) -> Option<usize>`: prefix match for directory entries
       - All helper functions should be `pub(crate)` or private as appropriate. `parse_gsd_phases` is `pub`.

    3. Add `pub mod gsd_phase_adapter;` to sentrux-core/src/analysis/mod.rs.

    4. Add `DiffWindow::CommitRange { from: String, to: String }` variant to DiffWindow enum in git_walker.rs. Add serde Serialize/Deserialize. Handle in all existing match arms in walk_git_log_windowed (CommitRange uses git2 OID range walk: resolve `from` OID, resolve `to` OID, walk commits between them). Also handle in DiffWindow Display/Debug if implemented. Add #[serde(default)] on CommitRange fields if needed for backward compat. NOTE: DiffWindow already derives Serialize/Deserialize — CommitRange with String fields works.

    5. Add ScanMsg::GsdPhaseReady(GsdPhaseReport) and ScanMsg::GsdPhaseError(String) to channels.rs. Handle in scanning.rs poll_scan_messages (log + repaint pattern, same as GitDiffReady). Update scanning_tests.rs match arm.

    Use `regex` crate (already in Cargo.toml dependency tree) for ROADMAP parsing. Do NOT add serde_yaml.
  </action>
  <verify>
    <automated>cargo test -p sentrux-core --lib gsd_phase 2>&1 | tail -30</automated>
  </verify>
  <done>All GSD phase types compile. Parser extracts phases, files, and commit ranges from .planning/ directory. DiffWindow::CommitRange exists. ScanMsg variants added. Tests pass for parsing, normalization, and directory matching.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ColorMode::GsdPhase variant, color functions, and RenderContext field</name>
  <files>
    sentrux-core/src/layout/types.rs,
    sentrux-core/src/renderer/colors.rs,
    sentrux-core/src/renderer/rects.rs,
    sentrux-core/src/renderer/mod.rs,
    sentrux-core/src/app/update_loop.rs
  </files>
  <behavior>
    - Test: gsd_phase_color(Completed) returns greenish (g > r)
    - Test: gsd_phase_color(InProgress) returns amber (r > b, g > b)
    - Test: gsd_phase_color(Planned) returns bluish (b > r)
    - Test: ColorMode::ALL contains GsdPhase and has correct count (9 variants)
    - Test: ColorMode::GsdPhase label() returns "GSD Phase"
    - Test: ColorMode::GsdPhase serializes to "GsdPhase" (not consumed by serde(other))
  </behavior>
  <action>
    1. Add `#[serde(rename = "GsdPhase")] GsdPhase` variant to ColorMode enum in layout/types.rs — insert BEFORE `#[serde(other)] Monochrome`. Update ALL const array to include GsdPhase (now 9 entries). Add label() match arm returning "GSD Phase".

    2. Add `gsd_phase_color(status: PhaseStatus) -> Color32` to colors.rs:
       - Completed: Color32::from_rgb(76, 153, 76) — muted green
       - InProgress: Color32::from_rgb(220, 165, 32) — amber/goldenrod
       - Planned: Color32::from_rgb(70, 130, 180) — steel blue
       Import PhaseStatus from pmat_types.

    3. Add `color_by_gsd_phase(ctx, path) -> Color32` to rects.rs:
       - No report → ctx.theme_config.file_surface (monochrome fallback)
       - Exact path lookup in report.by_file, with find_directory_match() fallback for directory prefix entries
       - No match → colors::NO_DATA_GRAY (GSDP-04: muted gray for unassociated)
       - Match → colors::gsd_phase_color(phase.status)
       Add `ColorMode::GsdPhase => color_by_gsd_phase(ctx, path)` arm to file_color() match.
       Import find_directory_match from gsd_phase_adapter (or duplicate the small helper locally to avoid cross-module import — use judgment).

    4. Add `pub gsd_phase_report: Option<&'a GsdPhaseReport>` field to RenderContext in renderer/mod.rs. Import GsdPhaseReport. Add `gsd_phase_report: None` placeholder in update_loop.rs RenderContext constructor (Plan 02 wires real data).

    5. Update any test asserting ColorMode::ALL length (e.g., `color_mode_all_has_exactly_N_variants`) to expect 9.
  </action>
  <verify>
    <automated>cargo test -p sentrux-core --lib 2>&1 | tail -30</automated>
  </verify>
  <done>ColorMode::GsdPhase exists before Monochrome, serializes correctly. Three-state phase colors render. color_by_gsd_phase dispatches in file_color(). RenderContext has gsd_phase_report field (None placeholder). All existing tests pass with updated variant count.</done>
</task>

</tasks>

<verification>
- `cargo test -p sentrux-core --lib 2>&1 | tail -30` — all tests pass
- `cargo build -p sentrux-core 2>&1 | tail -10` — compiles cleanly
- New tests verify: ROADMAP parsing, PLAN/SUMMARY file extraction, path normalization, phase colors, ColorMode variant
</verification>

<success_criteria>
- GsdPhaseReport, PhaseInfo, PhaseStatus types compile and are tested
- parse_gsd_phases() correctly reads .planning/ directory structure
- ColorMode::GsdPhase dispatches to color_by_gsd_phase() returning correct three-state colors
- DiffWindow::CommitRange variant compiles and serializes
- ScanMsg::GsdPhaseReady/GsdPhaseError handled in scanning.rs
- All existing tests pass (27 pre-existing oracle failures unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-phase-overlay/04-01-SUMMARY.md`
</output>
