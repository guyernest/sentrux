---
phase: 04-gsd-phase-overlay
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - sentrux-core/src/app/toolbar.rs
  - sentrux-core/src/app/draw_panels.rs
autonomous: false
requirements:
  - GSDP-02
  - GSDP-05

must_haves:
  truths:
    - "Phase navigator panel shows clickable phase timeline with colored segments (green/amber/blue)"
    - "Clicking a phase in navigator sets GitDiff window to that phase's commit range"
    - "Hovering a colored treemap node shows tooltip with phase number, name, and goal"
    - "Color legend shows GsdPhase three-state swatches when in GSD Phase mode"
    - "Completed and planned phases are visually distinct colors"
  artifacts:
    - path: "sentrux-core/src/app/draw_panels.rs"
      provides: "draw_gsd_phase_navigator() and draw_color_legend GsdPhase branch"
      contains: "draw_gsd_phase_navigator"
    - path: "sentrux-core/src/app/toolbar.rs"
      provides: "GsdPhase tooltip on hover showing phase info"
      contains: "gsd_phase_report"
  key_links:
    - from: "sentrux-core/src/app/draw_panels.rs"
      to: "sentrux-core/src/app/state.rs"
      via: "navigator reads gsd_phase_report and sets git_diff_window/git_diff_requested"
      pattern: "selected_phase_idx"
    - from: "sentrux-core/src/app/draw_panels.rs"
      to: "sentrux-core/src/renderer/colors.rs"
      via: "legend uses gsd_phase_color() for swatches"
      pattern: "gsd_phase_color"
---

<objective>
Build the GSD phase navigator panel, color legend integration, and hover tooltips — completing the user-facing interaction for the GSD phase overlay.

Purpose: Make the GSD phase overlay interactive — users can see which phase each file belongs to, hover for details, click phases to navigate git history, and understand the color scheme via the legend.
Output: Complete GSD phase overlay feature ready for visual verification.
</objective>

<execution_context>
@/Users/guy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gsd-phase-overlay/04-CONTEXT.md
@.planning/phases/04-gsd-phase-overlay/04-RESEARCH.md
@.planning/phases/04-gsd-phase-overlay/04-01-SUMMARY.md
@.planning/phases/04-gsd-phase-overlay/04-02-SUMMARY.md
@.planning/phases/03-git-diff-overlay/03-03-SUMMARY.md

<interfaces>
<!-- Key contracts from Plans 01-02 that this plan uses for UI -->

From sentrux-core/src/app/state.rs (Plan 02 adds):
```rust
pub gsd_phase_report: Option<GsdPhaseReport>,
pub gsd_phase_running: bool,
pub gsd_phase_requested: bool,
pub selected_phase_idx: Option<usize>,
```

From sentrux-core/src/core/pmat_types.rs (Plan 01 creates):
```rust
pub struct PhaseInfo {
    pub number: String,
    pub name: String,
    pub goal: String,
    pub status: PhaseStatus,
    pub completed_date: Option<String>,
    pub files: Vec<String>,
    pub commit_range: Option<(String, String)>,
}
pub enum PhaseStatus { Completed, InProgress, Planned }
```

From sentrux-core/src/renderer/colors.rs (Plan 01 creates):
```rust
pub fn gsd_phase_color(status: PhaseStatus) -> Color32;
```

From sentrux-core/src/metrics/evo/git_walker.rs (Plan 01 extends):
```rust
pub enum DiffWindow {
    TimeSecs(i64),
    CommitCount(u32),
    SinceLastTag,
    CommitRange { from: String, to: String },
}
```

From sentrux-core/src/app/draw_panels.rs (existing pattern):
```rust
fn draw_color_legend(ui: &mut egui::Ui, state: &AppState) {
    // dispatches by color_mode — add GsdPhase branch
}
```

From sentrux-core/src/app/toolbar.rs (existing pattern):
```rust
fn draw_git_diff_controls(ui: &mut egui::Ui, state: &mut AppState) {
    // only renders when color_mode == GitDiff — similar guard for GsdPhase navigator
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase navigator panel, color legend GsdPhase branch, and hover tooltip</name>
  <files>
    sentrux-core/src/app/draw_panels.rs,
    sentrux-core/src/app/toolbar.rs
  </files>
  <action>
    1. Add `draw_gsd_phase_navigator(ui: &mut egui::Ui, state: &mut AppState)` to draw_panels.rs:
       - Guard: only render when `state.color_mode == ColorMode::GsdPhase`
       - If no gsd_phase_report: show "No .planning/ directory found" as small weak RichText, return
       - If gsd_phase_running: show "Scanning GSD phases..." as small weak RichText, return
       - Render horizontal row of colored buttons, one per phase:
         - Button fill = gsd_phase_color(phase.status), text = "P{number} {name}" in white small RichText
         - Hover tooltip: "Phase {number}: {name}\nGoal: {goal}\nStatus: {status_label}\nFiles: {file_count}" — satisfies GSDP-05
         - If phase has commit_range: on click, set `state.git_diff_window = DiffWindow::CommitRange { from, to }` and `state.git_diff_requested = true` and `state.selected_phase_idx = Some(idx)` — this auto-syncs the GitDiff window to the clicked phase (unified time navigation per CONTEXT.md)
         - If phase has no commit_range: on click, just set selected_phase_idx (visual highlight only)
         - Highlight selected phase with a subtle border or different style
       - Place as status label, not full panel. Position: as a row rendered by draw_gsd_phase_controls() called from draw_toolbar() — same pattern as draw_git_diff_controls(). The function is called from toolbar.rs.
       - Use the naming `draw_gsd_phase_controls` for the toolbar function that calls into the navigator rendering.

    2. Add GsdPhase branch to `draw_color_legend()` in draw_panels.rs:
       - When ColorMode::GsdPhase: draw three swatches using painter.rect_filled():
         - Green swatch + "Completed" label
         - Amber swatch + "In Progress" label
         - Blue swatch + "Planned" label
         - Gray swatch + "Unassociated" label
       - Follow the same swatch rendering pattern used by GitDiff/TdgGrade/Coverage/Risk legends

    3. Wire `draw_gsd_phase_controls(ui, state)` call in toolbar.rs draw_toolbar():
       - Add call after `draw_git_diff_controls(ui, state)` — both are mode-gated so only one shows at a time
       - The function should be defined in toolbar.rs or draw_panels.rs (use judgment — if simple enough, define in toolbar.rs like draw_git_diff_controls; if complex, define in draw_panels.rs and call from toolbar.rs)

    4. Add hover tooltip for GSD phase info on treemap file nodes:
       - In the existing tooltip rendering code (check where hovered_path tooltip is drawn — likely in draw_panels.rs or a canvas interaction handler), when ColorMode::GsdPhase is active and the hovered file has a phase association, append phase info to the tooltip: "Phase {number}: {name} ({status})"
       - This satisfies GSDP-05: "Hovering a colored node shows which phase and its goal"
       - If the tooltip system is in renderer code that only has RenderContext access, the gsd_phase_report field on RenderContext (wired in Plan 02) provides the data
  </action>
  <verify>
    <automated>cargo build -p sentrux-core 2>&1 | tail -10</automated>
  </verify>
  <done>Phase navigator shows colored phase buttons with hover tooltips. Clicking a phase with commit_range triggers GitDiff for that range. Color legend shows three-state GSD swatches. File hover tooltip shows phase info. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete GSD phase overlay</name>
  <action>Human verifies the complete GSD phase overlay visually.</action>
  <verify>User approves visual verification</verify>
  <done>User confirms GSD phase overlay works correctly</done>
  <what-built>Complete GSD phase overlay: treemap coloring by phase (green/amber/blue/gray), phase navigator panel with clickable timeline, color legend, hover tooltips showing phase info, and phase-to-GitDiff navigation.</what-built>
  <how-to-verify>
    1. Run `cargo run` (or `just run` if available) to launch sentrux
    2. Open this project (sentrux itself) — it has a `.planning/` directory
    3. Switch to "GSD Phase" color mode via the toolbar color mode selector
    4. Verify treemap shows:
       - Files from completed phases (1, 2, 2.1, 3) in green
       - Files from Phase 4 (in-progress) in amber
       - Files not in any phase in muted gray
    5. Check the color legend below the toolbar shows: Completed (green), In Progress (amber), Planned (blue), Unassociated (gray)
    6. Check the phase navigator row shows colored buttons for each phase (P1 Cleanup, P2 PMAT Integration, P2.1 Rust Deep Analysis, P3 Git Diff Overlay, P4 GSD Phase Overlay)
    7. Hover a colored file — verify tooltip shows phase number, name, and goal
    8. Click a completed phase button (e.g., P3) — verify GitDiff mode activates showing that phase's commit range
    9. Close and reopen app — verify GSD Phase mode is restored from preferences
    10. Open a project WITHOUT .planning/ directory — verify graceful fallback (monochrome or "No .planning/ found" message)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cargo build -p sentrux-core 2>&1 | tail -10` — compiles
- `cargo test -p sentrux-core --lib 2>&1 | tail -30` — all tests pass
- Visual verification of complete GSD phase overlay (checkpoint task)
</verification>

<success_criteria>
- Phase navigator panel visible with colored clickable phase buttons
- Color legend shows three-state GSD swatches
- Hovering colored treemap nodes shows phase number, name, and goal
- Clicking phase triggers GitDiff for that phase's commit range (unified navigation)
- Completed phases visually distinct from planned phases (GSDP-02)
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-phase-overlay/04-03-SUMMARY.md`
</output>
